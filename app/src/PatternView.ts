import * as p5 from 'p5';

import RenderObject from "./RenderObject";

export default class Pattern extends RenderObject {
    render(sketch: p5): void {

    }

    protected update(sketch: p5): void {
    }

}
